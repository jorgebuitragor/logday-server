services:
  server:
    build: .
    ports:
      - "8080:8080"
    environment:
      DATABASE_PATH: /data/logday.db
    volumes:
      - logday-data:/data
    restart: unless-stopped

volumes:
  logday-data:
